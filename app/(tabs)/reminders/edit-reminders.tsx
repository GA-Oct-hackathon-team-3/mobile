import EditReminders from "../../../components/screens/EditReminders";
import { SafeAreaProvider } from "react-native-safe-area-context";

const Page = () => {
  return <EditReminders />;
};

export default Page;
